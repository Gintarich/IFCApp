<UserControl x:Class="IFCApp.UI.View.Sections.Parameters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IFCApp.UI.View.Sections"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="{StaticResource White}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Model" Margin="10 10" VerticalAlignment="Center" FontSize="16" FontWeight="Medium" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Boxes" Margin="10 10" VerticalAlignment="Center" FontSize="16" FontWeight="Medium"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ModelName}" Margin="10 5" Padding="3 3" Style="{StaticResource MyTextBox}"/>
            <Button 
                Grid.Row="1"
                Grid.Column="1" 
                Margin="20 5 20 5" 
                Width="auto" 
                Padding="3 3" 
                Content="Manage boxes" 
                Command="{Binding OpenBoxModalCommand}"
                Style="{StaticResource HorPanelButton}"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Parameters" Margin="10 10" VerticalAlignment="Center" FontSize="16" FontWeight="Medium"/>
            <Button 
                Grid.Row="2"
                Grid.Column="1" 
                Margin="20 5" 
                Width="auto" 
                Padding="3 3" 
                Content="Manage parameters" 
                Command="{Binding OpenBoxModalCommand}"
                Style="{StaticResource HorPanelButton}"/>
        <Button 
            Style="{StaticResource HorPanelButton}"
            Grid.Column="0"
            Grid.ColumnSpan="2" 
            Grid.Row="3" 
            Margin="10 15"
            Padding="10 5"
            Width="auto"
            Content="Pievienot parametrus" 
            Command="{Binding AddParametersCommand}" 
            HorizontalContentAlignment="Center"
            HorizontalAlignment="Left"/>
        <!--Margin="10"-->
        <!--Style="{StaticResource HorPanelButton}"-->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Trūkstošie elementi klasifikācijā"/>
        <TextBox Height="100" TextWrapping="Wrap" AcceptsReturn="True" HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto" Text="{Binding Errors}" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="4"/>

    </Grid>
</UserControl>
